#include <iostream>
using namespace std;
int dfs(int x,int y,int sum){
	if(sum>=3) return res;
	dfs(x)
}
int main(){
	scanf("%d",&n);
	for(int i=1;i<n;i++){
		scanf("%d %d",&x[i],&y[i])
	} 
  	printf("%lld",dfs(x[1],1,1));
  	return 0;
}
